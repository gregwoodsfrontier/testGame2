import { BootScene } from './boot-scene';
import { GameScene } from './game-scene';
import { TitleScene } from './title-scene';

const scenes = [BootScene, TitleScene, GameScene];

export default scenes;
